// This will be where a user can change the display name
// They can change the clock in some way (make it a round clock with a second hand maybe)
// They can clear out their to do list
// They can put in their link to their google calendar